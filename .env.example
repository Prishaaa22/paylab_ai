# PAYLAB AI environment variables
# The public demo works fully without this key (deterministic fallback).
# Add a Gemini API key to enable live AI root-cause analysis and fix generation.
GEMINI_API_KEY=
